{
  "name": "mcp-server-calc-tools",
  "version": "0.1.0",
  "description": "Advanced calculus tools for MCP servers",
  "bin": {
    "mcp-server-calc-tools": "./bin/mcp-server-calc-tools"
  },
  "scripts": {
    "postinstall": "bash build.sh",
    "test": "uv pip install pytest && python -m pytest tests/",
    "build": "uv pip compile requirements.txt -o requirements.lock"
  },
  "author": "Nbiish Justin Kenwabikise <nbiish@umich.edu>",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=16.0.0"
  }
}
